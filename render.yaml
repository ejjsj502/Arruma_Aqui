services:
  - type: web
    name: arruma-aqui
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn arruma_aqui.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: arruma-aqui-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4

databases:
  - name: arruma-aqui-db
    plan: free
